services:
  ros2_snake:
    build:
      context: .
    image: ros2-snake:humble
    environment:
      - DISPLAY=${DISPLAY:-host.docker.internal:0.0}
      - SDL_VIDEODRIVER=x11
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
    stdin_open: true
    tty: true
